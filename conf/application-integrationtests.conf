include "secure"

mongodb = {
  host = "localhost"
  host = ${?MONGODB_IP}
  port = 27017
  db = "io_logs"
  username = "mongodb"
  password = "mongodb"
}

rabbitmq = {
  host = "localhost"
  host = ${?RABBITMQ_IP}
  port = 5672
  virtualHost = "/"
  user = "rabbitmq"
  password = "rabbitmq"
  queue = {
    name = "ingestion_queue_integrationtests",
    routingKey = "io_log.ingestion"
  }

}