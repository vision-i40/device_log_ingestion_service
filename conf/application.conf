include "secure"

mongodb = {
  db = "io_logs_local"
  uri = "mongodb://localhost/"${mongodb.db}
}

rabbitmq = {
  host = "localhost"
  port = 5672
  virtualHost = "/"
  user = "rabbitmq"
  password = "rabbitmq"
  queue = {
    name = "ingestion_queue",
    routingKey = "io_log.ingestion"
  }

}