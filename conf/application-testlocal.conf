include "secure"

mongodb = {
  host = "localhost"
  port = 27017
  db = "io_logs_testlocal"
}

rabbitmq = {
  host = "localhost"
  port = 5672
  virtualHost = "/"
  user = "rabbitmq"
  password = "rabbitmq"
  queue = {
    name = "ingestion_queue_testlocal",
    routingKey = "io_log.ingestion"
  }

}